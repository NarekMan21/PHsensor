# PHsensor

🌐 **GitHub Pages**: [Демо-страница проекта](https://narekman21.github.io/PHsensor/)

✅ **Подключение с GitHub Pages**: 
- **Для публичных IP**: Приложение автоматически использует CORS прокси
- **Для локальных IP (192.168.x.x)**: Используйте HTTPS туннель (ngrok, Cloudflare Tunnel) - см. [TUNNEL_SETUP.md](TUNNEL_SETUP.md)
- **Самый простой способ**: Откройте ESP8266 напрямую по IP адресу - он имеет встроенный веб-сервер!

⚠️ **Проблемы с npoint.io?** 
- Если получаете ошибку 404 при записи данных, см. [NPOINT_SETUP.md](NPOINT_SETUP.md)
- Нужно создать новый endpoint на npoint.io с правами записи

Проект ESP8266 для мониторинга параметров почвы с использованием датчика PH-7-1 (7 параметров в одном).

## Возможности

- 📊 Мониторинг 7 параметров почвы:
  - Температура (°C)
  - Влажность (%)
  - Электропроводность (µS/cm)
  - pH
  - Азот (N) в mg/kg
  - Фосфор (P) в mg/kg
  - Калий (K) в mg/kg

- 🌐 Веб-интерфейс в стиле Home Assistant
- 📡 Интеграция с MQTT для Home Assistant
- 📱 Адаптивный дизайн для мобильных устройств
- 🔄 Автоматическое обновление данных каждые 5 секунд
- 📊 JSON API для получения данных

## Технические характеристики

- **Микроконтроллер**: ESP8266
- **Протокол связи с датчиком**: Modbus RTU через RS485
- **Скорость RS485**: 4800 бод
- **Интервал публикации MQTT**: 30 секунд
- **Веб-сервер**: Встроенный HTTP сервер на порту 80

## Установка

1. Установите необходимые библиотеки в Arduino IDE:
   - ESP8266WiFi
   - ESP8266WiFiMulti
   - ESP8266WebServer
   - PubSubClient
   - SoftwareSerial
   - ArduinoJson

2. Настройте параметры в коде:
   - WiFi SSID и пароль
   - MQTT сервер и учетные данные
   - RS485 пины (если необходимо)

3. Загрузите код на ESP8266

4. Откройте веб-интерфейс по IP адресу ESP8266 (отображается в Serial Monitor)

## Использование

### Веб-интерфейс

**✅ Рекомендуемый способ - Прямое подключение:**
ESP8266 имеет встроенный веб-сервер с полным интерфейсом! Просто откройте в браузере IP адрес ESP8266:
- `http://192.168.1.22` (или ваш IP адрес, который отображается в Serial Monitor)

**Преимущества:**
- ✅ Работает без проблем с HTTPS/HTTP
- ✅ Идентичный интерфейс
- ✅ Прямое подключение к датчику
- ✅ Автоматическое обновление каждые 5 секунд

**Вариант 2 - GitHub Pages (только для демонстрации):**
Откройте [демо-страницу](https://narekman21.github.io/PHsensor/) для просмотра интерфейса. 
⚠️ **Ограничение**: Для локальных IP адресов (192.168.x.x) публичные CORS прокси не могут подключиться, 
так как локальные сети недоступны из интернета. Используйте прямое подключение по IP адресу ESP8266.

**Вариант 3 - Локальный файл:**
Скачайте `index.html` и откройте его в браузере (работает как локальный файл, но также может иметь проблемы с CORS).

**💡 Важно:** ESP8266 уже содержит встроенный веб-сервер - просто откройте его IP адрес в браузере!

### JSON API

Для получения данных в формате JSON используйте:
```
GET http://<ESP8266_IP>/api
```

Пример ответа:
```json
{
  "temperature": 25.5,
  "humidity": 60.2,
  "conductivity": 1500,
  "pH": 6.8,
  "nitrogen": 120,
  "phosphorus": 80,
  "potassium": 200,
  "valid": true,
  "timestamp": 12345678
}
```

### MQTT

Данные автоматически публикуются в следующие топики:
- `esp8266/npkphcth/temperature`
- `esp8266/npkphcth/humidity`
- `esp8266/npkphcth/conductivity`
- `esp8266/npkphcth/ph`
- `esp8266/npkphcth/nitrogen`
- `esp8266/npkphcth/phosphorus`
- `esp8266/npkphcth/potassium`

## Подключение

### RS485
- RX: D5 (GPIO14)
- TX: D6 (GPIO12)
- RE: D1 (GPIO5)
- DE: D2 (GPIO4)

## GitHub Pages Deployment

Этот проект развернут на GitHub Pages. Для обновления:

1. Внесите изменения в файлы
2. Закоммитьте и запушьте изменения:
```bash
git add .
git commit -m "Update web interface"
git push origin main
```

3. GitHub Pages автоматически обновит сайт

## Лицензия

MIT

## Автор

Проект для мониторинга параметров почвы с использованием ESP8266 и датчика PH-7-1.

